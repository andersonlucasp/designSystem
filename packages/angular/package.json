{
  "name": "@ds/angular",
  "version": "0.1.0",
  "description": "Wrappers Angular do Design System (gerados pelo Stencil)",
  "license": "MIT",
  "publishConfig": { "access": "public" },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "peerDependencies": {
    "@angular/core": ">=16",
    "@angular/common": ">=16"
  },
  "dependencies": {
    "@ds/components": "0.1.0"
  },
  "devDependencies": {
    "@stencil/angular-output-target": "^0.10.1",
    "@angular/core": "^18.2.0",
    "@angular/common": "^18.2.0",
    "rxjs": "^7.8.0",
    "zone.js": "^0.14.0"
  },
  "nx": {
    "targets": { "build": { "dependsOn": ["^build"], "outputs": ["{projectRoot}/dist"] } }
  }
}
